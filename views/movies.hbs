<div class="container">
        <h1>Ironhack Cinema</h1>
        <h2>Click on the movie to check the showtimes!</h2>
        {{#each movies}}
        <img src="{{image}}"/>
        <h2>{{title}}</h2>
        <a href="/movies/{{_id}}">See more</a>
        {{/each}}
    </div>
</section>